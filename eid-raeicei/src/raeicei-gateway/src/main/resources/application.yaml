server:
  port: ${RAEICEI_GW_SERVER_PORT}
  ssl:
    enabled: false
    trust-store: ${RAEICEI_GW_TRUST_STORE}
    trust-store-password: ${RAEICEI_GW_TRUST_STORE_PASSWORD}
    trust-store-type: PKCS12
logging:
  secret-key: ${RAEICEI_GW_AUDIT_LOGGING_SECRET_KEY}
  encryption-key:
  module-id: raeicei-gateway
  system-id: raeicei
  file-name-pattern-audit: ${RAEICEI_AUDIT_LOG_FILE_NAME}
  file-name-pattern-audit-unsigned: ${RAEICEI_AUDIT_LOG_FILE_NAME_UNSIGNED}
  max-file-size-audit: ${RAEICEI_AUDIT_MAX_FILE_SIZE}
  max-file-history-audit: ${RAEICEI_AUDIT_MAX_FILE_HISTORY}
  file-name-pattern: ${RAEICEI_BE_LOG_FILE_NAME}
springdoc:
  swagger-ui:
    enabled: ${RAEICEI_SWAGGER_ENABLED}
    disable-swagger-default-url: true
    path: /docs
spring:
  main:
    web-application-type: reactive
  codec:
    max-in-memory-size: 10MB
  profiles:
    active: ${RAEICEI_GW_SPRING_ACTIVE_PROFILES}
    group:
      test: test,logging-test
mvr:
  application-file-store: /hopr[wepor