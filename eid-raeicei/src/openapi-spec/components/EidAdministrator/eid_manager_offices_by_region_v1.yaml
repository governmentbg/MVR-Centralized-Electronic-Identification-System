schemas:
  OfficesByRegionDTO:
    type: object
    properties:
      region:
        type: string
      from:
        type: string
        format: date
      to:
        type: string
        format: date